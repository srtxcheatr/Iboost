# Android / Gradle
*.iml
.gradle/
/local.properties
.idea/
.DS_Store
/build
/captures
.externalNativeBuild/
.cxx/
*.apk
*.aab

# Keystores — never commit your real signing key
*.keystore
*.jks
release.keystore

# Gradle wrapper jar isn't committed (see BUILD_INSTRUCTIONS.md) —
# generate it locally with `gradle wrapper` or let Android Studio do it.
